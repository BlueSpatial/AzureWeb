myApp.component('userLoginComponent', {
    // isolated scope binding
   
    template: '',

    // The controller that handles our component logic
    controller: ['$rootScope', function ($rootScope) {
        var $ctrl = this;
        
    }]
});