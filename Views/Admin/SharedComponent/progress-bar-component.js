myApp.component('progressBarComponent', {
    bindings: {
       
    },

    templateUrl: '/Views/Admin/SharedComponent/progress-bar-component.html',

    controller: ['$http', '$rootScope', "$scope", 'authorizeService', 'layerService', function ($http, $rootScope, $scope, authorizeService, layerService) {
        var $ctrl = this;
       
        
    }]
});